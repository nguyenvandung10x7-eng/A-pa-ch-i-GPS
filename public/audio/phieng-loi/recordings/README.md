# Recording drop folder

Place the cleared human dialogue and vocal-Foley takes here using the exact names
in `docs/phieng-loi-audio-recording-library.md`. Then document provenance and mark
the matching cue `ready` in `src/experiences/phieng-loi/audioManifest.ts`.

Do not commit recordings made from TTS or imitations of named performers.
